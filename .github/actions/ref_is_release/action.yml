name: "Determine is a ref relates to a Github release"
description: "Given a ref, determine if it is a release (matches the release tag format)."
outputs:
  is_release:
    description: "Boolean string, 'true' if it matches a release tag format, 'false' otherwise."
runs:
  using: "node20"
  main: "index.js"
