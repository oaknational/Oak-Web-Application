mutation createUser($userId: String!) {
  insert_users(
    objects: [{ id: $userId }]
    on_conflict: { constraint: users_pkey, update_columns: [updated_at] }
  ) {
    returning {
      id
      updated_at
      created_at
    }
  }
}
