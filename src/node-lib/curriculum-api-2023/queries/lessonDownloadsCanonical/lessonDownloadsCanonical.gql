query lessonDownloadsCanonical($lessonSlug: String!) {
  lessonDownloadsCanonical: published_mv_downloads_7(
    where: { lessonSlug: { _eq: $lessonSlug } }
  ) {
    downloads
    programmeSlug
    keyStageSlug
    keyStageTitle
    lessonSlug
    lessonTitle
    subjectSlug
    subjectTitle
    unitSlug
    unitTitle
  }
}
