query subjectListing($keyStageSlug: String!) {
  keyStageSubjects: published_mv_subject_listing_5(
    where: { keyStageSlug: { _eq: $keyStageSlug } }
  ) {
    keyStageSlug
    keyStageTitle
    subjects
    subjectsUnavailable
    keyStages
  }
}
