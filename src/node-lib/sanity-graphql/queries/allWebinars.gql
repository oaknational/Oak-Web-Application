query allWebinars($isDraft: Boolean = false, $limit: Int = 9999) {
  allWebinar(
    where: { _: { is_draft: $isDraft } }
    sort: [{ date: DESC }]
    limit: $limit
  ) {
    ...WebinarPreviewFields
  }
}
