import { act, renderHook } from "@testing-library/react-hooks";

import { AllTheProviders } from "../../__tests__/__helpers__/renderWithProviders";

import { useToggleMenu } from "./MenuProvider";

describe("useToggleMenu", () => {
  test("is closed on start", () => {
    fail("stub");
  });

  test("can be opened", () => {
    fail("stub");
  });

  test("can be closed", () => {
    fail("stub");
  });
});
