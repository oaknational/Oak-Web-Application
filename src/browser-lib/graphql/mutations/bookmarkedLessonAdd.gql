mutation bookmarkedLessonAdd($lessonId: uuid!) {
  insert_bookmarkedLessons_one(object: { lessonId: $lessonId }) {
    lessonId
    userId
    lesson {
      id
      slug
      title
    }
    createdAt
  }
}
