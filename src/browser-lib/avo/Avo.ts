// Generated by Avo VERSION 114.102.0. You should never have to make changes to this file.
// If you find yourself in the situation where you have to edit the file please contact us at hi@avo.app.
// If you encounter a git conflict in this file run `avo pull` and it will be resolved automatically.
/* tslint:disable */
/* eslint-disable */
// @ts-nocheck

// fetch() polyfill
(function () {
  if (typeof window === 'undefined') {
    return;
  }
  var support = {
    searchParams: 'URLSearchParams' in self,
    iterable: 'Symbol' in self && 'iterator' in Symbol,
    blob:
      'FileReader' in self &&
      'Blob' in self &&
      (function () {
        try {
          new Blob();
          return true
        } catch (e) {
          return false
        }
      })(),
    formData: 'FormData' in self,
    arrayBuffer: 'ArrayBuffer' in self
  };

  function isDataView(obj: any) {
    return obj && DataView.prototype.isPrototypeOf(obj)
  }

  if (support.arrayBuffer) {
    var viewClasses = [
      '[object Int8Array]',
      '[object Uint8Array]',
      '[object Uint8ClampedArray]',
      '[object Int16Array]',
      '[object Uint16Array]',
      '[object Int32Array]',
      '[object Uint32Array]',
      '[object Float32Array]',
      '[object Float64Array]'
    ];

    var isArrayBufferView =
      ArrayBuffer.isView ||
      function (obj) {
        return obj && viewClasses.indexOf(Object.prototype.toString.call(obj)) > -1
      };
  }

  function normalizeName(name: any) {
    if (typeof name !== 'string') {
      name = String(name);
    }
    if (/[^a-z0-9\-#$%&'*+.^_`|~]/i.test(name)) {
      throw new TypeError('Invalid character in header field name')
    }
    return name.toLowerCase()
  }

  function normalizeValue(value: any) {
    if (typeof value !== 'string') {
      value = String(value);
    }
    return value
  }

  // Build a destructive iterator for the value list
  function iteratorFor(items: any) {
    var iterator: any = {
      next: function () {
        var value = items.shift();
        return { done: value === undefined, value: value }
      }
    };

    if (support.iterable) {
      iterator[Symbol.iterator] = function () {
        return iterator
      };
    }

    return iterator
  }

  function Headers(headers: any) {
    // @ts-ignore
    (this as any).map = {};

    if (headers instanceof Headers) {
      (headers as any).forEach(function (value: any, name: any) {
        // @ts-ignore
        this.append(name, value);
        // @ts-ignore
      }, this);
    } else if (Array.isArray(headers)) {
      headers.forEach(function (header: any) {
        // @ts-ignore
        this.append(header[0], header[1]);
        // @ts-ignore
      }, this);
    } else if (headers) {
      Object.getOwnPropertyNames(headers).forEach(function (name: any) {
        // @ts-ignore
        this.append(name, headers[name]);
        // @ts-ignore
      }, this);
    }
  }

  Headers.prototype.append = function (name: any, value: any) {
    name = normalizeName(name);
    value = normalizeValue(value);
    var oldValue = this.map[name];
    this.map[name] = oldValue ? oldValue + ', ' + value : value;
  };

  Headers.prototype['delete'] = function (name: any) {
    delete this.map[normalizeName(name)];
  };

  Headers.prototype.get = function (name: any) {
    name = normalizeName(name);
    return this.has(name) ? this.map[name] : null
  };

  Headers.prototype.has = function (name: any) {
    return this.map.hasOwnProperty(normalizeName(name))
  };

  Headers.prototype.set = function (name: any, value: any) {
    this.map[normalizeName(name)] = normalizeValue(value);
  };

  Headers.prototype.forEach = function (callback: any, thisArg: any) {
    for (var name in this.map) {
      if (this.map.hasOwnProperty(name)) {
        callback.call(thisArg, this.map[name], name, this);
      }
    }
  };

  Headers.prototype.keys = function () {
    var items: any = [];
    this.forEach(function (_value: any, name: any) {
      items.push(name);
    });
    return iteratorFor(items)
  };

  Headers.prototype.values = function () {
    var items: any = [];
    this.forEach(function (value: any) {
      items.push(value);
    });
    return iteratorFor(items)
  };

  Headers.prototype.entries = function () {
    var items: any = [];
    this.forEach(function (value: any, name: any) {
      items.push([name, value]);
    });
    return iteratorFor(items)
  };

  if (support.iterable) {
    Headers.prototype[Symbol.iterator] = Headers.prototype.entries;
  }

  function consumed(body: any) {
    if (body.bodyUsed) {
      return true;
    }
    body.bodyUsed = true;
    return false;
  }

  function fileReaderReady(reader: any) {
    return new Promise(function (resolve: any, reject: any) {
      reader.onload = function () {
        resolve(reader.result);
      };
      reader.onerror = function () {
        reject(reader.error);
      };
    })
  }

  function readBlobAsArrayBuffer(blob: any) {
    var reader = new FileReader();
    var promise = fileReaderReady(reader);
    reader.readAsArrayBuffer(blob);
    return promise
  }

  function readBlobAsText(blob: any) {
    var reader = new FileReader();
    var promise = fileReaderReady(reader);
    reader.readAsText(blob);
    return promise
  }

  function readArrayBufferAsText(buf: any) {
    var view = new Uint8Array(buf);
    var chars = new Array(view.length);

    for (var i = 0; i < view.length; i++) {
      chars[i] = String.fromCharCode(view[i]!);
    }
    return chars.join('')
  }

  function bufferClone(buf: any) {
    if (buf.slice) {
      return buf.slice(0)
    } else {
      var view = new Uint8Array(buf.byteLength);
      view.set(new Uint8Array(buf));
      return view.buffer
    }
  }

  function Body() {
    // @ts-ignore
    (this as any).bodyUsed = false;

    // @ts-ignore
    (this as any)._initBody = function (body: any) {
      this._bodyInit = body;
      if (!body) {
        this._bodyText = '';
      } else if (typeof body === 'string') {
        this._bodyText = body;
      } else if (support.blob && Blob.prototype.isPrototypeOf(body)) {
        this._bodyBlob = body;
      } else if (support.formData && FormData.prototype.isPrototypeOf(body)) {
        this._bodyFormData = body;
      } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
        this._bodyText = body.toString();
      } else if (support.arrayBuffer && support.blob && isDataView(body)) {
        this._bodyArrayBuffer = bufferClone(body.buffer);
        // IE 10-11 can't handle a DataView body.
        this._bodyInit = new Blob([this._bodyArrayBuffer]);
      } else if (support.arrayBuffer && (ArrayBuffer.prototype.isPrototypeOf(body) || isArrayBufferView(body))) {
        this._bodyArrayBuffer = bufferClone(body);
      } else {
        this._bodyText = body = Object.prototype.toString.call(body);
      }

      if (!this.headers.get('content-type')) {
        if (typeof body === 'string') {
          this.headers.set('content-type', 'text/plain;charset=UTF-8');
        } else if (this._bodyBlob && this._bodyBlob.type) {
          this.headers.set('content-type', this._bodyBlob.type);
        } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
          this.headers.set('content-type', 'application/x-www-form-urlencoded;charset=UTF-8');
        }
      }
    };

    if (support.blob) {
      // @ts-ignore
      (this as any).blob = function () {
        var rejected = consumed(this);
        if (rejected) {
          return Promise.reject(new TypeError('Already read'));
        }

        if (this._bodyBlob) {
          return Promise.resolve(this._bodyBlob);
        } else if (this._bodyArrayBuffer) {
          return Promise.resolve(new Blob([this._bodyArrayBuffer]));
        } else if (this._bodyFormData) {
          throw new Error('could not read FormData body as blob');
        } else {
          return Promise.resolve(new Blob([this._bodyText]));
        }
      };

      // @ts-ignore
      (this as any).arrayBuffer = function () {
        if (this._bodyArrayBuffer) {
          if (consumed(this)) {
            return Promise.reject(new TypeError('Already read'));
          } else {
            return Promise.resolve(this._bodyArrayBuffer);
          }
        } else {
          return this.blob().then(readBlobAsArrayBuffer);
        }
      };
    }

    // @ts-ignore
    (this as any).text = function () {
      var rejected = consumed(this);
      if (rejected) {
        return Promise.reject(new TypeError('Already read'));
      }

      if (this._bodyBlob) {
        return readBlobAsText(this._bodyBlob);
      } else if (this._bodyArrayBuffer) {
        return Promise.resolve(readArrayBufferAsText(this._bodyArrayBuffer));
      } else if (this._bodyFormData) {
        throw new Error('could not read FormData body as text');
      } else {
        return Promise.resolve(this._bodyText);
      }
    };

    if (support.formData) {
      // @ts-ignore
      (this as any).formData = function () {
        return this.text().then(decode)
      };
    }

    // @ts-ignore
    (this as any).json = function () {
      return this.text().then(JSON.parse)
    };

    // @ts-ignore
    return this
  }

  // HTTP methods whose capitalization should be normalized
  var methods = ['DELETE', 'GET', 'HEAD', 'OPTIONS', 'POST', 'PUT'];

  function normalizeMethod(method: any) {
    var upcased = method.toUpperCase();
    return methods.indexOf(upcased) > -1 ? upcased : method
  }

  function Request(input: any, options: any) {
    options = options || {};
    var body = options.body;

    if (input instanceof Request) {
      if ((input as any).bodyUsed) {
        throw new TypeError('Already read')
      }
      // @ts-ignore
      (this as any).url = (input as any).url;
      // @ts-ignore
      this.credentials = (input as any).credentials;
      if (!options.headers) {
        // @ts-ignore
        this.headers = new Headers((input as any).headers);
      }
      // @ts-ignore
      this.method = (input as any).method;
      // @ts-ignore
      this.mode = (input as any).mode;
      // @ts-ignore
      this.signal = (input as any).signal;
      if (!body && (input as any)._bodyInit != null) {
        body = (input as any)._bodyInit;
        (input as any).bodyUsed = true;
      }
    } else {
      // @ts-ignore
      this.url = String(input);
    }

    // @ts-ignore
    this.credentials = options.credentials || this.credentials || 'same-origin';
    // @ts-ignore
    if (options.headers || !this.headers) {
      // @ts-ignore
      this.headers = new Headers(options.headers);
    }
    // @ts-ignore
    this.method = normalizeMethod(options.method || this.method || 'GET');
    // @ts-ignore
    this.mode = options.mode || this.mode || null;
    // @ts-ignore
    this.signal = options.signal || this.signal;
    // @ts-ignore
    this.referrer = null;

    // @ts-ignore
    if ((this.method === 'GET' || this.method === 'HEAD') && body) {
      throw new TypeError('Body not allowed for GET or HEAD requests')
    }
    // @ts-ignore
    this._initBody(body);
  }

  Request.prototype.clone = function () {
    // @ts-ignore
    return new Request(this, { body: this._bodyInit })
  };

  function decode(body: any) {
    var form = new FormData();
    body
      .trim()
      .split('&')
      .forEach(function (bytes: any) {
        if (bytes) {
          var split = bytes.split('=');
          var name = split.shift().replace(/\+/g, ' ');
          var value = split.join('=').replace(/\+/g, ' ');
          form.append(decodeURIComponent(name), decodeURIComponent(value));
        }
      });
    return form
  }

  function parseHeaders(rawHeaders: any) {
    // @ts-ignore
    var headers = new Headers();
    // Replace instances of \r\n and \n followed by at least one space or horizontal tab with a space
    // https://tools.ietf.org/html/rfc7230#section-3.2
    var preProcessedHeaders = rawHeaders.replace(/\r?\n[\t ]+/g, ' ');
    preProcessedHeaders.split(/\r?\n/).forEach(function (line: any) {
      var parts = line.split(':');
      var key = parts.shift().trim();
      if (key) {
        var value = parts.join(':').trim();
        headers.append(key, value);
      }
    });
    return headers
  }

  Body.call(Request.prototype);

  function Response(bodyInit: any, options: any) {
    if (!options) {
      options = {};
    }

    // @ts-ignore
    this.type = 'default';
    // @ts-ignore
    this.status = options.status === undefined ? 200 : options.status;
    // @ts-ignore
    this.ok = this.status >= 200 && this.status < 300;
    // @ts-ignore
    this.statusText = 'statusText' in options ? options.statusText : 'OK';
    // @ts-ignore
    this.headers = new Headers(options.headers);
    // @ts-ignore
    this.url = options.url || '';
    // @ts-ignore
    this._initBody(bodyInit);
  }

  Body.call(Response.prototype);

  Response.prototype.clone = function () {
    // @ts-ignore
    return new Response(this._bodyInit, {
      status: this.status,
      statusText: this.statusText,
      // @ts-ignore
      headers: new Headers(this.headers),
      url: this.url
    })
  };

  Response.error = function () {
    // @ts-ignore
    var response = new Response(null, { status: 0, statusText: '' });
    response.type = 'error';
    return response
  };

  var redirectStatuses = [301, 302, 303, 307, 308];

  Response.redirect = function (url: any, status: any) {
    if (redirectStatuses.indexOf(status) === -1) {
      throw new RangeError('Invalid status code')
    }

    // @ts-ignore
    return new Response(null, { status: status, headers: { location: url } })
  };

  (self as any).DOMException = (self as any).DOMException;
  try {
    new (self as any).DOMException();
  } catch (err) {
    (self as any).DOMException = function (message: any, name: any) {
      this.message = message;
      this.name = name;
      var error = Error(message);
      this.stack = error.stack;
    };
    (self as any).DOMException.prototype = Object.create(Error.prototype);
    (self as any).DOMException.prototype.constructor = (self as any).DOMException;
  }

  function fetch(input: any, init: any) {
    return new Promise(function (resolve, reject) {
      // @ts-ignore
      var request = new Request(input, init);

      if (request.signal && request.signal.aborted) {
        return reject(new (self as any).DOMException('Aborted', 'AbortError'))
      }

      var xhr = new XMLHttpRequest();

      function abortXhr() {
        xhr.abort();
      }

      xhr.onload = function () {
        var options = {
          status: xhr.status,
          statusText: xhr.statusText,
          headers: parseHeaders(xhr.getAllResponseHeaders() || '')
        };
        (options as any).url = 'responseURL' in xhr ? xhr.responseURL : options.headers.get('X-Request-URL');
        var body = 'response' in xhr ? xhr.response : (xhr as any).responseText;
        // @ts-ignore
        resolve(new Response(body, options));
      };

      xhr.onerror = function () {
        reject(new TypeError('Network request failed'));
      };

      xhr.ontimeout = function () {
        reject(new TypeError('Network request failed'));
      };

      xhr.onabort = function () {
        reject(new (self as any).DOMException('Aborted', 'AbortError'));
      };

      xhr.open(request.method, request.url, true);

      if (request.credentials === 'include') {
        xhr.withCredentials = true;
      } else if (request.credentials === 'omit') {
        xhr.withCredentials = false;
      }

      if ('responseType' in xhr && support.blob) {
        xhr.responseType = 'blob';
      }

      request.headers.forEach(function (value: any, name: any) {
        xhr.setRequestHeader(name, value);
      });

      if (request.signal) {
        request.signal.addEventListener('abort', abortXhr);

        xhr.onreadystatechange = function () {
          // DONE (success or failure)
          if (xhr.readyState === 4) {
            request.signal.removeEventListener('abort', abortXhr);
          }
        };
      }

      xhr.send(typeof request._bodyInit === 'undefined' ? null : request._bodyInit);
    })
  }

  fetch.polyfill = true;

  if (!self.fetch) {
    (self as any).fetch = fetch;
    (self as any).Headers = Headers;
    (self as any).Request = Request;
    (self as any).Response = Response;
  }
})();

export enum AvoEnv {
  Prod = "prod",
  Dev = "dev",
}

export interface CustomDestination {
  make?(env: AvoEnv, apiKey: string): void;
  logEvent?: (eventName: string, eventProperties: Record<string, any>) => void;
  setUserProperties?: (userId: string, userProperties: Record<string, any>) => void;
  identify?: (userId: string) => void;
  unidentify?: () => void;
  logPage?: (pageName: string, eventProperties: Record<string, any>) => void;
  revenue?: (amount: number, eventProperties: Record<string, any>) => void;
  setGroupProperties?: (
    groupType: string,
    groupId: string,
    groupProperties: Record<string, any>,
  ) => void;
  addCurrentUserToGroup?: (
    groupType: string,
    groupId: string,
    groupProperties: Record<string, any>,
  ) => void;
  logEventWithGroups?: (
    eventName: string,
    eventProperties: any,
    groupTypesToGroupIds: Record<string, string>,
  ) => void;
}

// @ts-ignore
interface AvoAssertMessage {
  eventName?: string;
  tag?: string;
  propertyId?: string;
  message?: string;
  additionalProperties?: string[],
  shape?: any,
  shapeUserProps?: any,
  actualType?: string
}

let __AVO_ENV__: AvoEnv | null = null;
// @ts-ignore
let __AVO_NOOP__: boolean = false;
// @ts-ignore
let __AVO_LOGGER__: AvoLogger | null = null;
// @ts-ignore
let __STRICT__: boolean | null = null;
// @ts-ignore
let __REPORT_FAILURE_AS__: 'error' | 'warn' | 'log' | null = null;

// @ts-ignore
let __WEB_DEBUGGER__: boolean = true;
export const avoInspectorApiKey = "2GQRkd2H3Rnk4FV16NaK";
// @ts-ignore
interface AvoInspector {}
let __INSPECTOR__: AvoInspector | null = null;


// polyfill Object.assign
// @ts-ignore
declare interface ObjectConstructor {
  assign: any;
}
// @ts-ignore
if (typeof Object.assign !== 'function') {
  // Must be writable: true, enumerable: false, configurable: true
  Object.defineProperty(Object, "assign", {
    value: function assign(target: any, _varArgs: any) { // .length of function is 2
      if (target == null) { // TypeError if undefined or null
        throw new TypeError('Cannot convert undefined or null to object');
      }

      let to = Object(target);

      for (let index = 1; index < arguments.length; index++) {
        let nextSource = arguments[index];

        if (nextSource != null) { // Skip over if undefined or null
          for (let nextKey in nextSource) {
            // Avoid bugs when hasOwnProperty is shadowed
            if (Object.prototype.hasOwnProperty.call(nextSource, nextKey)) {
              to[nextKey] = nextSource[nextKey];
            }
          }
        }
      }
      return to;
    },
    writable: true,
    configurable: true
  });
}

interface AvoLogger {
  logDebug(env: AvoEnv | null, message: string): boolean;
  logWarn(env: AvoEnv | null, message: string): boolean;
  logError(env: AvoEnv | null, error: string): boolean;
}

enum webDebuggerArea {
  BottomRight = "BottomRight",
  BottomLeft = "BottomLeft",
  TopRight = "TopRight",
  TopLeft = "TopLeft"
}

interface bottomRightParameters {
  bottom: number;
  right: number;
}

interface bottomLeftParameters {
  bottom: number;
  left: number;
}

interface topRightParameters {
  top: number;
  right: number;
}

interface topLeftParameters {
  top: number;
  left: number;
}

interface webDebuggerPosition {
  position: webDebuggerArea;
  top?: number;
  right?: number;
  bottom?: number;
  left?: number;
}

interface webDebuggerOptions {
  position?: webDebuggerPosition;
}

interface WebDebuggerPositionSetter {
  BottomRight(p: bottomRightParameters): webDebuggerPosition;
  BottomLeft(p: bottomLeftParameters): webDebuggerPosition;
  TopRight(p: topRightParameters): webDebuggerPosition;
  TopLeft(p: topLeftParameters): webDebuggerPosition;
}

export const WebDebuggerPosition: WebDebuggerPositionSetter = {
  BottomRight: ({ bottom, right }) => ({
    position: webDebuggerArea.BottomRight,
    bottom,
    right,
  }),
  BottomLeft: ({ bottom, left }) => ({
    position: webDebuggerArea.BottomLeft,
    bottom,
    left,
  }),
  TopRight: ({ top, right }) => ({
    position: webDebuggerArea.TopRight,
    top,
    right,
  }),
  TopLeft: ({ top, left }) => ({
    position: webDebuggerArea.TopLeft,
    top,
    left,
  }),
}


let InternalAvoLogger: any = {
  logEventSent: function logEventSent(eventName: string, eventProperties: any, userProperties: any) {
    const message = "Event Sent:" + eventName + "Event Props:" + JSON.stringify(eventProperties) + "User Props:" + JSON.stringify(userProperties);

    if (__AVO_LOGGER__ && __AVO_LOGGER__.logDebug && __AVO_LOGGER__.logDebug(__AVO_ENV__, message)) {
      return
    }
    typeof console !== 'undefined' && console.log("[avo] Event Sent:", eventName, "Event Props:", eventProperties, "User Props:", userProperties);
  },

  log: function log(message: string) {
    if (__AVO_LOGGER__ && __AVO_LOGGER__.logDebug && __AVO_LOGGER__.logDebug(__AVO_ENV__, message)) {
      return
    }
    typeof console !== 'undefined' && console.log("[avo] " + message);
  },

  warn: function warn(message: string) {
    if (__AVO_LOGGER__ && __AVO_LOGGER__.logWarn && __AVO_LOGGER__.logWarn(__AVO_ENV__, message)) {
      return
    }
    typeof console !== 'undefined' && console.warn("[avo] " + message);
  },

  error: function error(message: string, error: string) {
    if (__AVO_LOGGER__ && __AVO_LOGGER__.logError && __AVO_LOGGER__.logError(__AVO_ENV__, message + error)) {
      return
    }
    typeof console !== 'undefined' && console.error("[avo] " + message, error);
  }
};

// @ts-ignore
let array_difference: any;
// @ts-ignore
let AvoAssert: any;
array_difference = function array_difference(a1: any[], a2: any[]) {
  let result: any[] = [];
  for (let i = 0; i < a1.length; i++) {
    if (a2.indexOf(a1[i]) === -1) {
      result.push(a1[i]);
    }
  }
  return result;
}

AvoAssert = {
  assertObject: function assertObject(propertyId: string, propName: string, obj: any) {
    if (typeof obj !== 'object') {
      let message = propName +
          ' should be of type object but you provided type ' +
          typeof obj +
          ' with value ' +
          JSON.stringify(obj);
      return [{tag: 'expectedObjectType', propertyId, message, actualType: typeof obj}];
    } else {
      return [];
    }
  },

  assertString: function assertString(propertyId: string, propName: string, str: string) {
    if (typeof str !== 'string') {
      let message = propName +
          ' should be of type string but you provided type ' +
          typeof str +
          ' with value ' +
          JSON.stringify(str);
      return [{tag: 'expectedStringType', propertyId, message, actualType: typeof str}];
    } else {
      return [];
    }
  },

  assertInt: function assertInt(propertyId: string, propName: string, int: number) {
    if (typeof int === 'number' && int !== Math.round(int)) {
      let message = propName +
          ' should be of type int but you provided type float with value ' +
          JSON.stringify(int);
      return [{tag: 'expectedIntType', propertyId, message, actualType: 'float'}];
    } else if (typeof int !== 'number') {
      let message = propName +
          ' should be of type int but you provided type ' +
          typeof int +
          ' with value ' +
          JSON.stringify(int);
      return [{tag: 'expectedIntType', propertyId, message, actualType: typeof int}];
    } else {
      return [];
    }
  },

  assertLong: function assertLong(propertyId: string, propName: string, long: number) {
    if (typeof long === 'number' && long !== Math.round(long)) {
      let message = propName +
          ' should be of type long but you provided type float with value ' +
          JSON.stringify(long);
      return [{tag: 'expectedLongType', propertyId, message, actualType: 'float'}];
    } else if (typeof long !== 'number') {
      let message = propName +
          ' should be of type long but you provided type ' +
          typeof long +
          ' with value ' +
          JSON.stringify(long);
      return [{tag: 'expectedLongType', propertyId, message, actualType: typeof long}];
    } else {
      return [];
    }
  },

  assertFloat: function assertFloat(propertyId: string, propName: string, float: number) {
    if (typeof float !== 'number') {
      let message = propName +
          ' should be of type float but you provided type ' +
          typeof float +
          ' with value ' +
          JSON.stringify(float);
      return [{tag: 'expectedFloatType', propertyId, message, actualType: typeof float}];
    } else {
      return [];
    }
  },

  assertBool: function assertBool(propertyId: string, propName: string, bool: boolean) {
    if (typeof bool !== 'boolean') {
      let message = propName +
          ' should be of type boolean but you provided type ' +
          typeof bool +
          ' with value ' +
          JSON.stringify(bool);
      return [{tag: 'expectedBoolType', propertyId, message, actualType: typeof bool}];
    } else {
      return [];
    }
  },

  assertMax: function assertMax(
    propertyId: string,
    propName: string,
    max: number,
    value: number
  ) {
    if (value > max) {
      let message = propName +
        ' has a maximum value of ' +
        max +
        ' but you provided the value ' +
        JSON.stringify(value);
      return [{tag: 'expectedMax', propertyId, message}];
    } else {
      return [];
    }
  },

  assertMin: function assertMin(
    propertyId: string,
    propName: string,
    min: number,
    value: number
  ) {
    if (value < min) {
      let message = propName +
        ' has a minimum value of ' +
        min +
        ' but you provided the value ' +
        JSON.stringify(value);
      return [{tag: 'expectedMin', propertyId, message}];
    } else {
      return [];
    }
  },

  assertList: function assertList(propertyId: string, propName: string, value: any) {
    if (!Array.isArray(value)) {
      let message = propName + ' should be of type list but you provided type ' + typeof value;
      return [{tag: 'expectedList', propertyId, message}];
    } else {
      return [];
    }
  },

  assertNoAdditionalProperties: function assertNoAdditionalProperties(eventName: string, input: string[], spec: string[]) {
    let additionalKeys = array_difference(input, spec);
    if (additionalKeys.length) {
      let message = "Additional properties when sending event " + eventName + ": " + JSON.stringify(additionalKeys);
      return [{tag: 'expectedNoAdditionalProperties', additionalProperties: additionalKeys, message: message}];
    } else {
      return [];
    }
  },

  assertNoAdditionalUserProperties: function assertNoAdditionalProperties(eventName: string, input: string[], spec: string[]) {
    let additionalKeys = array_difference(input, spec);
    if (additionalKeys.length) {
      let message = "Additional user properties when sending event " + eventName + ": " + JSON.stringify(additionalKeys);
      return [{tag: 'expectedNoAdditionalUserProperties', additionalProperties: additionalKeys, message: message}];
    } else {
      return [];
    }
  },
};

let _avo_invoke: any;
let _avo_invoke_meta: any;
let _avo_sampling_rate = 1.0;
_avo_invoke = function _avo_invoke(env: AvoEnv, eventId: string, hash: string, messages: {tag: string, propertyId: string}[], origin: string) {
  // @ts-ignore
  if (typeof (window as any) === 'undefined') { return; }
  if (_avo_sampling_rate > 0) {
    if (Math.random() < _avo_sampling_rate) {
      // @ts-ignore
      fetch("https://api.avo.app/i", {
        method: "POST",
        headers: {"Content-Type": "application/json"},
        body: JSON.stringify({
          "ac": "txWMKnNSaZnc3rVbixqO",
          "br": "cjVJMBMUT",
          "en": env,
          "ev": eventId,
          "ha": hash,
          "sc": "5PhajbVijwhXVKIJtGMT",
          "se": (new Date()).toISOString(),
          "so": "OXY1Uf7nb",
          "va": messages.length === 0,
          "me": messages,
          "or": origin
        })
      }).then(function(res: any) { return res.json(); }).then(function(data: any) { _avo_sampling_rate = data.sa; }).catch(function() {});
    }
  }
}

_avo_invoke_meta = function _avo_invoke_meta(env: AvoEnv, type: string, messages: {tag: string, propertyId: string}[], origin: string) {
  // @ts-ignore
  if (typeof (window as any) === 'undefined') { return; }
  if (_avo_sampling_rate > 0) {
    if (Math.random() < _avo_sampling_rate) {
      // @ts-ignore
      fetch("https://api.avo.app/i", {
        method: "POST",
        headers: {"Content-Type": "application/json"},
        body: JSON.stringify({
          "ac": "txWMKnNSaZnc3rVbixqO",
          "br": "cjVJMBMUT",
          "en": env,
          "ty": type,
          "sc": "5PhajbVijwhXVKIJtGMT",
          "se": (new Date()).toISOString(),
          "so": "OXY1Uf7nb",
          "va": messages.length === 0,
          "me": messages,
          "or": origin
        })
      }).then(function(res: any) { return res.json(); }).then(function(data: any) { _avo_sampling_rate = data.sa; }).catch(function() {});
    }
  }
}


let _avo_debugger_log: any;
let _avo_debugger_events_during_boot: any = [];
let _avo_debugger_ready = false;

if (typeof (window as any) !== 'undefined') {
  window.addEventListener("message", function(event) {
    if (event.origin !== "https://www.avo.app") {
      return;
    }
    let iframe: any = document.getElementById("avo-debugger");
    if (iframe && event && event.data && event.data.type_ === "avo-debugger-update-style") {
      iframe.style = event.data.style;
    } else if (iframe && event && event.data && event.data.type_ === "avo-debugger-ready") {
      let message = {
        type_: "avo-debugger-boot-events",
        schemaId: "5PhajbVijwhXVKIJtGMT",
        href: window.location.href,
        events: _avo_debugger_events_during_boot
      };
      _avo_debugger_events_during_boot = [];
      _avo_debugger_ready = true;
      iframe.contentWindow.postMessage(message, "https://www.avo.app/_debugger")
    }
  });
}

_avo_debugger_log = function _avo_debugger_log(eventId: string, eventName: string, messages: any[], eventProperties: any[], userProperties: any[], groupProperties: any[]) {
  if (typeof (window as any) === 'undefined') { return; }
  let event = {
    eventId: eventId,
    eventName: eventName,
    messages: messages,
    timestamp: Date.now(),
    eventProperties,
    userProperties,
    groupProperties
  };

  if (_avo_debugger_ready) {
    let message = {type_: "avo-debugger-events", events: [event]};
    (document.getElementById("avo-debugger") as any).contentWindow.postMessage(message, "https://www.avo.app/_debugger")
  } else {
    _avo_debugger_events_during_boot.push(event);
  }
}

function _avo_debugger_send_position(position: webDebuggerPosition) {
  if (typeof window === 'undefined') { return; }
  window.addEventListener("message", function(event) {
    if (event.data.type_ === "avo-debugger-ready" && position !== null) {
      var message = {type_: "avo-debugger-position", position: position};
      document.getElementById("avo-debugger").contentWindow.postMessage(message, "https://www.avo.app/_debugger")
    }
  })
}


export const VideoLocation = {
  'WEBINAR': 'webinar',
  'MARKETING': 'marketing',
  'LESSON': 'lesson',
  'BLOG': 'blog',
} as const;
export type VideoLocationType = typeof VideoLocation;
export type VideoLocationValueType = VideoLocationType[keyof VideoLocationType];

export const AnalyticsUseCase = {
  'PUPIL': 'Pupil',
  'TEACHER': 'Teacher',
} as const;
export type AnalyticsUseCaseType = typeof AnalyticsUseCase;
export type AnalyticsUseCaseValueType = AnalyticsUseCaseType[keyof AnalyticsUseCaseType];

export const ResourceType = {
  'PRESENTATION': 'presentation',
  'INTRO_QUIZ_QUESTIONS': 'intro-quiz-questions',
  'INTRO_QUIZ_ANSWERS': 'intro-quiz-answers',
  'EXIT_QUIZ_QUESTIONS': 'exit-quiz-questions',
  'EXIT_QUIZ_ANSWERS': 'exit-quiz-answers',
  'WORKSHEET_PDF': 'worksheet-pdf',
  'WORKSHEET_PPTX': 'worksheet-pptx',
} as const;
export type ResourceTypeType = typeof ResourceType;
export type ResourceTypeValueType = ResourceTypeType[keyof ResourceTypeType];

export const PageName = {
  'SUBJECT_LISTING': 'Subject Listing',
  'UNIT_LISTING': 'Unit Listing',
  'HOMEPAGE': 'Homepage',
  'LESSON_LISTING': 'Lesson Listing',
  'LESSON': 'Lesson',
  'WEBINAR': 'Webinar',
  'BLOG': 'Blog',
  'PLAN_A_LESSON': 'Plan a Lesson',
  'DEVELOP_YOUR_CURRICULUM': 'Develop Your Curriculum',
  'SUPPORT_YOUR_TEAM': 'Support Your Team',
  'SEARCH': 'Search',
  'LESSON_DOWNLOAD': 'Lesson Download',
  'UNIT_DOWNLOAD': 'Unit Download',
} as const;
export type PageNameType = typeof PageName;
export type PageNameValueType = PageNameType[keyof PageNameType];

export const NavigatedFrom = {
  'HEADER': 'header',
  'FOOTER': 'footer',
  'CARD': 'card',
  'MENU': 'menu',
} as const;
export type NavigatedFromType = typeof NavigatedFrom;
export type NavigatedFromValueType = NavigatedFromType[keyof NavigatedFromType];

export const KeyStageTitle = {
  'KEY_STAGE_1': 'Key stage 1',
  'KEY_STAGE_2': 'Key stage 2',
  'KEY_STAGE_3': 'Key stage 3',
  'KEY_STAGE_4': 'Key stage 4',
} as const;
export type KeyStageTitleType = typeof KeyStageTitle;
export type KeyStageTitleValueType = KeyStageTitleType[keyof KeyStageTitleType];

let PostHog: any;

export function initAvo(options: {env: AvoEnv; webDebugger?: boolean;
  webDebuggerOptions?: webDebuggerOptions; strict?: boolean; noop?: boolean;
  reportFailureAs?: 'error' | 'warn' | 'log'; inspector?: AvoInspector;
  avoLogger?: AvoLogger}, destinationOptions: any,
  PostHogDestination: CustomDestination) {
  if (__AVO_ENV__ !== null) {
    return;
  }
  __AVO_ENV__ = options.env;
  if (options.avoLogger !== undefined) {
    __AVO_LOGGER__ = options.avoLogger;
  }
  if (options.noop === true) {
    __AVO_NOOP__ = true;
  }
  if (__AVO_NOOP__ && __AVO_ENV__ == AvoEnv.Prod) {
    InternalAvoLogger.warn("[avo] ****************************************************");
    InternalAvoLogger.warn("[avo] WARNING Avo cannot be initialized in noop mode in production:");
    InternalAvoLogger.warn("[avo] - Overwriting configuration with noop=false.");
    InternalAvoLogger.warn("[avo] - Please reach out if you want to be able to run Avo in production mode with noop=true");
    InternalAvoLogger.warn("[avo] ****************************************************");
    __AVO_NOOP__ = false;
  }
  if (__AVO_NOOP__) {
    InternalAvoLogger.log("[avo] ****************************************************");
    InternalAvoLogger.log("[avo] Avo is now initialized in noop mode. This means:");
    InternalAvoLogger.log("[avo] - No events will be sent");
    InternalAvoLogger.log("[avo] - No network requests are made");
    InternalAvoLogger.log("[avo] ****************************************************");
  }
  if (options.strict !== undefined) {
    __STRICT__ = options.strict !== false;
  }
  if (options.reportFailureAs !== undefined) {
    __REPORT_FAILURE_AS__ = options.reportFailureAs;
  }
  __WEB_DEBUGGER__ = !__AVO_NOOP__ && ((typeof window !== 'undefined' && (window as any).location.search.indexOf("avo_debug=1") > -1) || (options.webDebugger !== false && __AVO_ENV__ !== AvoEnv.Prod));
  if (!__AVO_NOOP__ && options.inspector !== undefined) {
    __INSPECTOR__ = options.inspector;
  } else if (__AVO_ENV__ !== 'prod') {
    InternalAvoLogger.warn("[avo] Avo Inspector not provided in initAvo() call");
  }

  destinationOptions = destinationOptions || {};

  if (__WEB_DEBUGGER__ && !__AVO_NOOP__) {
    if (options.webDebuggerOptions?.position) {
      _avo_debugger_send_position(options.webDebuggerOptions.position)
    }

(function() {
  if (typeof (window as any) === 'undefined') { return; }
  let init = function() {
    let iframe: any = document.createElement("iframe");
    document.body.appendChild(iframe);
    iframe.id = "avo-debugger";
    iframe.src = "https://www.avo.app/_debugger";
    iframe.style = "display: none;";
  };

  if (document.body) {
    init();
  } else {
    document.addEventListener('DOMContentLoaded', init);
  }
})();

  }
  if (!__AVO_NOOP__) {
    if (__AVO_ENV__ === AvoEnv.Prod) {
    }
    if (__AVO_ENV__ === AvoEnv.Dev) {
    }

    PostHog = PostHogDestination;
    if (__AVO_ENV__ === 'prod') {
      PostHog && PostHog.make && PostHog.make(__AVO_ENV__, null);
    } else if (__AVO_ENV__ === 'dev') {
      PostHog && PostHog.make && PostHog.make(__AVO_ENV__, null);
    } else {
      console[__REPORT_FAILURE_AS__ || 'error']("[avo] No staging key is set for PostHog. Head to destination settings in Avo to set a staging key.");
      PostHog && PostHog.make && PostHog.make(__AVO_ENV__, null);
    }
    if (__AVO_ENV__ === AvoEnv.Dev) {
      // debug console in Avo
      _avo_invoke_meta(__AVO_ENV__, 'init', [], 'init');
    }
  }
}

export function setAvoLogger(avoLogger: AvoLogger | null) {
  __AVO_LOGGER__ = avoLogger;
}

/**
 * Plan a Lesson Selected: Plan a lesson card is selected from the launch homepage
 *
 * When to trigger this event:
 * 1.
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/6FJxOlnzBp/trigger/Hgd_u_6rr
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/6FJxOlnzBp}
 */
export function planALessonSelected() {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "6FJxOlnzBp", "b45359cd914e124cea6ba08974be2f89593bb0a184959fe59b7c5e92c6bf78fa", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Plan a Lesson Selected", {}, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("6FJxOlnzBp", "Plan a Lesson Selected", messages, [], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Plan a Lesson Selected", {}, "6FJxOlnzBp", "b45359cd914e124cea6ba08974be2f89593bb0a184959fe59b7c5e92c6bf78fa");
    }
    // destination PostHog
    PostHog.logEvent("Plan a Lesson Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

/**
 * Newsletter Sign Up Completed: A visitor submits the form to subscribe to the newsletter
 *
 * When to trigger this event:
 * 1. User has completed the newsletter fields and pressed the Sign Up button
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/sLqKBjU-Wt/trigger/_MC_apwfp
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/sLqKBjU-Wt}
 */
export function newsletterSignUpCompleted() {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "sLqKBjU-Wt", "934048affdfdaed482322016de455d1d2d2ba209baa1d36542093999d7045053", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Newsletter Sign Up Completed", {}, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("sLqKBjU-Wt", "Newsletter Sign Up Completed", messages, [], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Newsletter Sign Up Completed", {}, "sLqKBjU-Wt", "934048affdfdaed482322016de455d1d2d2ba209baa1d36542093999d7045053");
    }
    // destination PostHog
    PostHog.logEvent("Newsletter Sign Up Completed", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface ClassroomSelectedProperties {
  navigatedFrom: NavigatedFromValueType;
}
/**
 * Classroom Selected: Classroom app is selected from the Launch Homepage (card or header or menu or footer)
 *
 * When to trigger this event:
 * 1. Classroom app selected from homepage card
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/esgLdNSBsj/trigger/d46nqqMBY
 * 2. Classroom app selected from menu
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/esgLdNSBsj/trigger/M_vqoTLFQ
 *
 * @param properties the properties associatied with this event
 * @param properties.navigatedFrom: The location of the link used to navigate to the current page
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/esgLdNSBsj}
 */
export function classroomSelected(properties: ClassroomSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "esgLdNSBsj", "2dd9eee7dfaec87984266c0f9bad30e55ed1fb61b81f6d29d6cc6861fdbf182f", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Classroom Selected", {
      "Navigated From": properties.navigatedFrom,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("esgLdNSBsj", "Classroom Selected", messages, [
      {id: "p48bLbzXJ-", name: "Navigated From", value: properties.navigatedFrom},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Navigated From"] = properties.navigatedFrom;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Classroom Selected", {
        "Navigated From": properties.navigatedFrom,
        }, "esgLdNSBsj", "2dd9eee7dfaec87984266c0f9bad30e55ed1fb61b81f6d29d6cc6861fdbf182f");
    }
    // destination PostHog
    PostHog.logEvent("Classroom Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface TeacherHubSelectedProperties {
  navigatedFrom: NavigatedFromValueType;
}
/**
 * Teacher Hub Selected: Teacher Hub app is selected from the Launch Homepage (card or menu)
 *
 * When to trigger this event:
 * 1. Teacher Hub selected from homepage card
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/kp-dTd4WU3/trigger/8sYQASFRC
 * 2. Teacher Hub selected from menu
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/kp-dTd4WU3/trigger/_JhfvPPiP
 *
 * @param properties the properties associatied with this event
 * @param properties.navigatedFrom: The location of the link used to navigate to the current page
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/kp-dTd4WU3}
 */
export function teacherHubSelected(properties: TeacherHubSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "kp-dTd4WU3", "4858b098eeadf5235cb4d7cef804979b85ec4e36a0f4b75ee5207e779312aaa2", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Teacher Hub Selected", {
      "Navigated From": properties.navigatedFrom,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("kp-dTd4WU3", "Teacher Hub Selected", messages, [
      {id: "p48bLbzXJ-", name: "Navigated From", value: properties.navigatedFrom},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Navigated From"] = properties.navigatedFrom;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Teacher Hub Selected", {
        "Navigated From": properties.navigatedFrom,
        }, "kp-dTd4WU3", "4858b098eeadf5235cb4d7cef804979b85ec4e36a0f4b75ee5207e779312aaa2");
    }
    // destination PostHog
    PostHog.logEvent("Teacher Hub Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

/**
 * Develop Your Curriculum Selected: Develop your curriculum card selected from launch homepage
 *
 * When to trigger this event:
 * 1.
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/pI9xLEeG6a/trigger/dRt9RWsfe
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/pI9xLEeG6a}
 */
export function developYourCurriculumSelected() {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "pI9xLEeG6a", "08b77edc6fd04cfa8d366887db7f3bd0db650dbafd1b8bf27a6bc305eeacfd3b", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Develop Your Curriculum Selected", {}, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("pI9xLEeG6a", "Develop Your Curriculum Selected", messages, [], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Develop Your Curriculum Selected", {}, "pI9xLEeG6a", "08b77edc6fd04cfa8d366887db7f3bd0db650dbafd1b8bf27a6bc305eeacfd3b");
    }
    // destination PostHog
    PostHog.logEvent("Develop Your Curriculum Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

/**
 * Support Your Team Selected: Support your team card selected from launch homepage
 *
 * When to trigger this event:
 * 1.
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/LqBk-euseD/trigger/F9AZuqUAA
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/LqBk-euseD}
 */
export function supportYourTeamSelected() {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "LqBk-euseD", "7b2310c5e7b49a54ade7d2b0c01f8ff6b23d32b5b71150e729496572c3c0274d", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Support Your Team Selected", {}, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("LqBk-euseD", "Support Your Team Selected", messages, [], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Support Your Team Selected", {}, "LqBk-euseD", "7b2310c5e7b49a54ade7d2b0c01f8ff6b23d32b5b71150e729496572c3c0274d");
    }
    // destination PostHog
    PostHog.logEvent("Support Your Team Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface NotificationSelectedProperties {
  linkUrl: string;
  notificationHeadline: string;
}
/**
 * Notification Selected: Notification card from upper right corner of launch homepage is selected
 *
 * When to trigger this event:
 * 1.
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/M_zZRmq4zA/trigger/8JSZfjNwi
 *
 * @param properties the properties associatied with this event
 * @param properties.linkUrl: The href of a link
 * @param properties.notificationHeadline: The heading/link text in a notification card
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/M_zZRmq4zA}
 */
export function notificationSelected(
  properties: NotificationSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "M_zZRmq4zA", "8a1a216285b74d8b86f32dcde6a4a899bdead1a15f3dccf7d53a94d91d0664c9", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Notification Selected", {
      "Link URL": properties.linkUrl,
      "Notification Headline": properties.notificationHeadline,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("M_zZRmq4zA", "Notification Selected", messages, [
      {id: "cJ1c_F_Qih", name: "Link URL", value: properties.linkUrl},
      {id: "QCncIcqjhM", name: "Notification Headline", value: properties.notificationHeadline},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Link URL"] = properties.linkUrl;
  eventProperties["Notification Headline"] = properties.notificationHeadline;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Notification Selected", {
        "Link URL": properties.linkUrl,
        "Notification Headline": properties.notificationHeadline,
        }, "M_zZRmq4zA", "8a1a216285b74d8b86f32dcde6a4a899bdead1a15f3dccf7d53a94d91d0664c9");
    }
    // destination PostHog
    PostHog.logEvent("Notification Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

/**
 * About Selected: About us is selected from the Launch homepage
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/Aj3imCEK7t}
 */
export function aboutSelected() {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "Aj3imCEK7t", "1b068c2468f56dfeef48251a2e45c5e4bc821580cd24615e546b5c1bb2db8e27", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("About Selected", {}, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("Aj3imCEK7t", "About Selected", messages, [], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("About Selected", {}, "Aj3imCEK7t", "1b068c2468f56dfeef48251a2e45c5e4bc821580cd24615e546b5c1bb2db8e27");
    }
    // destination PostHog
    PostHog.logEvent("About Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface VideoStartedProperties {
  durationSeconds: number | null | undefined;
  isCaptioned: boolean;
  videoPlaybackId: string;
  videoTitle: string;
  timeElapsedSeconds: number;
  isMuted: boolean;
  videoLocation: VideoLocationValueType | null | undefined;
}
/**
 * Video Started: A video is played for the first time after a user lands on a page
 *
 * @param properties the properties associatied with this event
 * @param properties.durationSeconds: Video length in seconds
 * @param properties.isCaptioned: Indicates whether captions were enabled when the event was triggered
 * @param properties.videoPlaybackId: Playback Id of a mux video
 * @param properties.videoTitle: Title of video
 * @param properties.timeElapsedSeconds: Progress made through a video in seconds
 * @param properties.isMuted: Indicates whether video was muted when the event was triggered
 * @param properties.videoLocation: Where is the video on the site (eg. webinar, marketing, lesson, blog, etc)
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/G0I28K0B2f}
 */
export function videoStarted(properties: VideoStartedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "G0I28K0B2f", "26f38167cccfbc3e5a65704fdc8b70ff00ac5dd47b1051c722b180c9fe2767b0", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Video Started", {
      "Duration (Seconds)": properties.durationSeconds,
      "Is Captioned": properties.isCaptioned,
      "Video Playback Id": properties.videoPlaybackId,
      "Video Title": properties.videoTitle,
      "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
      "Is Muted": properties.isMuted,
      "Video Location": properties.videoLocation,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("G0I28K0B2f", "Video Started", messages, [
      {id: "NqtddGscIh", name: "Duration (Seconds)", value: properties.durationSeconds},
      {id: "xHdFKUwmsd", name: "Is Captioned", value: properties.isCaptioned},
      {id: "ueSG_AEgt", name: "Video Playback Id", value: properties.videoPlaybackId},
      {id: "S98ZxfGtRh", name: "Video Title", value: properties.videoTitle},
      {id: "3a0mowzVyL", name: "Time Elapsed (Seconds)", value: properties.timeElapsedSeconds},
      {id: "8AC7R8LYpO", name: "Is Muted", value: properties.isMuted},
      {id: "62ypa-_ys", name: "Video Location", value: properties.videoLocation},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  if (properties.durationSeconds !== undefined && properties.durationSeconds !== null) {
    eventProperties["Duration (Seconds)"] = properties.durationSeconds;
  }
  eventProperties["Is Captioned"] = properties.isCaptioned;
  eventProperties["Video Playback Id"] = properties.videoPlaybackId;
  eventProperties["Video Title"] = properties.videoTitle;
  eventProperties["Time Elapsed (Seconds)"] = properties.timeElapsedSeconds;
  eventProperties["Is Muted"] = properties.isMuted;
  if (properties.videoLocation !== undefined && properties.videoLocation !== null) {
    eventProperties["Video Location"] = properties.videoLocation;
  }

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Video Started", {
        "Duration (Seconds)": properties.durationSeconds,
        "Is Captioned": properties.isCaptioned,
        "Video Playback Id": properties.videoPlaybackId,
        "Video Title": properties.videoTitle,
        "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
        "Is Muted": properties.isMuted,
        "Video Location": properties.videoLocation,
        }, "G0I28K0B2f", "26f38167cccfbc3e5a65704fdc8b70ff00ac5dd47b1051c722b180c9fe2767b0");
    }
    // destination PostHog
    PostHog.logEvent("Video Started", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface VideoPausedProperties {
  durationSeconds: number | null | undefined;
  isCaptioned: boolean;
  videoPlaybackId: string;
  videoTitle: string;
  timeElapsedSeconds: number;
  isMuted: boolean;
  videoLocation: VideoLocationValueType | null | undefined;
}
/**
 * Video Paused: A video is paused before it it finished
 *
 * @param properties the properties associatied with this event
 * @param properties.durationSeconds: Video length in seconds
 * @param properties.isCaptioned: Indicates whether captions were enabled when the event was triggered
 * @param properties.videoPlaybackId: Playback Id of a mux video
 * @param properties.videoTitle: Title of video
 * @param properties.timeElapsedSeconds: Progress made through a video in seconds
 * @param properties.isMuted: Indicates whether video was muted when the event was triggered
 * @param properties.videoLocation: Where is the video on the site (eg. webinar, marketing, lesson, blog, etc)
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/z91WauKeVB}
 */
export function videoPaused(properties: VideoPausedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "z91WauKeVB", "584956a675b18fd9fb50dfd54709e2b07d87c25c03382c966729cb974f785fac", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Video Paused", {
      "Duration (Seconds)": properties.durationSeconds,
      "Is Captioned": properties.isCaptioned,
      "Video Playback Id": properties.videoPlaybackId,
      "Video Title": properties.videoTitle,
      "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
      "Is Muted": properties.isMuted,
      "Video Location": properties.videoLocation,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("z91WauKeVB", "Video Paused", messages, [
      {id: "NqtddGscIh", name: "Duration (Seconds)", value: properties.durationSeconds},
      {id: "xHdFKUwmsd", name: "Is Captioned", value: properties.isCaptioned},
      {id: "ueSG_AEgt", name: "Video Playback Id", value: properties.videoPlaybackId},
      {id: "S98ZxfGtRh", name: "Video Title", value: properties.videoTitle},
      {id: "3a0mowzVyL", name: "Time Elapsed (Seconds)", value: properties.timeElapsedSeconds},
      {id: "8AC7R8LYpO", name: "Is Muted", value: properties.isMuted},
      {id: "62ypa-_ys", name: "Video Location", value: properties.videoLocation},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  if (properties.durationSeconds !== undefined && properties.durationSeconds !== null) {
    eventProperties["Duration (Seconds)"] = properties.durationSeconds;
  }
  eventProperties["Is Captioned"] = properties.isCaptioned;
  eventProperties["Video Playback Id"] = properties.videoPlaybackId;
  eventProperties["Video Title"] = properties.videoTitle;
  eventProperties["Time Elapsed (Seconds)"] = properties.timeElapsedSeconds;
  eventProperties["Is Muted"] = properties.isMuted;
  if (properties.videoLocation !== undefined && properties.videoLocation !== null) {
    eventProperties["Video Location"] = properties.videoLocation;
  }

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Video Paused", {
        "Duration (Seconds)": properties.durationSeconds,
        "Is Captioned": properties.isCaptioned,
        "Video Playback Id": properties.videoPlaybackId,
        "Video Title": properties.videoTitle,
        "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
        "Is Muted": properties.isMuted,
        "Video Location": properties.videoLocation,
        }, "z91WauKeVB", "584956a675b18fd9fb50dfd54709e2b07d87c25c03382c966729cb974f785fac");
    }
    // destination PostHog
    PostHog.logEvent("Video Paused", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface VideoPlayedProperties {
  durationSeconds: number | null | undefined;
  isCaptioned: boolean;
  videoPlaybackId: string;
  videoTitle: string;
  timeElapsedSeconds: number;
  isMuted: boolean;
  videoLocation: VideoLocationValueType | null | undefined;
}
/**
 * Video Played: A video is played again after being paused
 *
 * @param properties the properties associatied with this event
 * @param properties.durationSeconds: Video length in seconds
 * @param properties.isCaptioned: Indicates whether captions were enabled when the event was triggered
 * @param properties.videoPlaybackId: Playback Id of a mux video
 * @param properties.videoTitle: Title of video
 * @param properties.timeElapsedSeconds: Progress made through a video in seconds
 * @param properties.isMuted: Indicates whether video was muted when the event was triggered
 * @param properties.videoLocation: Where is the video on the site (eg. webinar, marketing, lesson, blog, etc)
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/r4DFADUHFh}
 */
export function videoPlayed(properties: VideoPlayedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "r4DFADUHFh", "d13924a9f3f7566d8cda683014da967123d7849a29c212a41ababbe13984fab3", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Video Played", {
      "Duration (Seconds)": properties.durationSeconds,
      "Is Captioned": properties.isCaptioned,
      "Video Playback Id": properties.videoPlaybackId,
      "Video Title": properties.videoTitle,
      "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
      "Is Muted": properties.isMuted,
      "Video Location": properties.videoLocation,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("r4DFADUHFh", "Video Played", messages, [
      {id: "NqtddGscIh", name: "Duration (Seconds)", value: properties.durationSeconds},
      {id: "xHdFKUwmsd", name: "Is Captioned", value: properties.isCaptioned},
      {id: "ueSG_AEgt", name: "Video Playback Id", value: properties.videoPlaybackId},
      {id: "S98ZxfGtRh", name: "Video Title", value: properties.videoTitle},
      {id: "3a0mowzVyL", name: "Time Elapsed (Seconds)", value: properties.timeElapsedSeconds},
      {id: "8AC7R8LYpO", name: "Is Muted", value: properties.isMuted},
      {id: "62ypa-_ys", name: "Video Location", value: properties.videoLocation},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  if (properties.durationSeconds !== undefined && properties.durationSeconds !== null) {
    eventProperties["Duration (Seconds)"] = properties.durationSeconds;
  }
  eventProperties["Is Captioned"] = properties.isCaptioned;
  eventProperties["Video Playback Id"] = properties.videoPlaybackId;
  eventProperties["Video Title"] = properties.videoTitle;
  eventProperties["Time Elapsed (Seconds)"] = properties.timeElapsedSeconds;
  eventProperties["Is Muted"] = properties.isMuted;
  if (properties.videoLocation !== undefined && properties.videoLocation !== null) {
    eventProperties["Video Location"] = properties.videoLocation;
  }

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Video Played", {
        "Duration (Seconds)": properties.durationSeconds,
        "Is Captioned": properties.isCaptioned,
        "Video Playback Id": properties.videoPlaybackId,
        "Video Title": properties.videoTitle,
        "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
        "Is Muted": properties.isMuted,
        "Video Location": properties.videoLocation,
        }, "r4DFADUHFh", "d13924a9f3f7566d8cda683014da967123d7849a29c212a41ababbe13984fab3");
    }
    // destination PostHog
    PostHog.logEvent("Video Played", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface VideoFinishedProperties {
  durationSeconds: number | null | undefined;
  isCaptioned: boolean;
  videoPlaybackId: string;
  videoTitle: string;
  timeElapsedSeconds: number;
  isMuted: boolean;
  videoLocation: VideoLocationValueType | null | undefined;
}
/**
 * Video Finished: The end of a video is reached
 *
 * @param properties the properties associatied with this event
 * @param properties.durationSeconds: Video length in seconds
 * @param properties.isCaptioned: Indicates whether captions were enabled when the event was triggered
 * @param properties.videoPlaybackId: Playback Id of a mux video
 * @param properties.videoTitle: Title of video
 * @param properties.timeElapsedSeconds: Progress made through a video in seconds
 * @param properties.isMuted: Indicates whether video was muted when the event was triggered
 * @param properties.videoLocation: Where is the video on the site (eg. webinar, marketing, lesson, blog, etc)
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/NP9klWkaki}
 */
export function videoFinished(properties: VideoFinishedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "NP9klWkaki", "fb5001186cc50c58af423f20a315e13db4e2a31c7a41da72a599ed147ce8f7f8", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Video Finished", {
      "Duration (Seconds)": properties.durationSeconds,
      "Is Captioned": properties.isCaptioned,
      "Video Playback Id": properties.videoPlaybackId,
      "Video Title": properties.videoTitle,
      "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
      "Is Muted": properties.isMuted,
      "Video Location": properties.videoLocation,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("NP9klWkaki", "Video Finished", messages, [
      {id: "NqtddGscIh", name: "Duration (Seconds)", value: properties.durationSeconds},
      {id: "xHdFKUwmsd", name: "Is Captioned", value: properties.isCaptioned},
      {id: "ueSG_AEgt", name: "Video Playback Id", value: properties.videoPlaybackId},
      {id: "S98ZxfGtRh", name: "Video Title", value: properties.videoTitle},
      {id: "3a0mowzVyL", name: "Time Elapsed (Seconds)", value: properties.timeElapsedSeconds},
      {id: "8AC7R8LYpO", name: "Is Muted", value: properties.isMuted},
      {id: "62ypa-_ys", name: "Video Location", value: properties.videoLocation},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  if (properties.durationSeconds !== undefined && properties.durationSeconds !== null) {
    eventProperties["Duration (Seconds)"] = properties.durationSeconds;
  }
  eventProperties["Is Captioned"] = properties.isCaptioned;
  eventProperties["Video Playback Id"] = properties.videoPlaybackId;
  eventProperties["Video Title"] = properties.videoTitle;
  eventProperties["Time Elapsed (Seconds)"] = properties.timeElapsedSeconds;
  eventProperties["Is Muted"] = properties.isMuted;
  if (properties.videoLocation !== undefined && properties.videoLocation !== null) {
    eventProperties["Video Location"] = properties.videoLocation;
  }

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Video Finished", {
        "Duration (Seconds)": properties.durationSeconds,
        "Is Captioned": properties.isCaptioned,
        "Video Playback Id": properties.videoPlaybackId,
        "Video Title": properties.videoTitle,
        "Time Elapsed (Seconds)": properties.timeElapsedSeconds,
        "Is Muted": properties.isMuted,
        "Video Location": properties.videoLocation,
        }, "NP9klWkaki", "fb5001186cc50c58af423f20a315e13db4e2a31c7a41da72a599ed147ce8f7f8");
    }
    // destination PostHog
    PostHog.logEvent("Video Finished", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface ResourcesDownloadedProperties {
  keyStageTitle: KeyStageTitleValueType;
  keyStageSlug: string;
  unitSlug: string;
  unitName: string;
  subjectTitle: string;
  subjectSlug: string;
  lessonName: string;
  lessonSlug: string;
  resourceType: ResourceTypeValueType[];
  analyticsUseCase: AnalyticsUseCaseValueType[];
}
/**
 * Resources Downloaded: A user downloaded one or more resources for a lesson.
 *
 * When to trigger this event:
 * 1. The download .zip button is clicked on the lesson download page and submission passes validation checks
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/k9ZQJai7ws/trigger/sAqordxVG
 *
 * @param properties the properties associatied with this event
 * @param properties.keyStageTitle: Title of the current key stage.
 * @param properties.keyStageSlug: Human-readable unique ID of the current key stage.
 * @param properties.unitSlug: Human-readable unique ID of the current unit.
 * @param properties.unitName: Title of the current unit.
 * @param properties.subjectTitle: Title of the current subject.
 * @param properties.subjectSlug: Human-readable unique ID of the current subject.
 * @param properties.lessonName: Name of the current lesson.
 * @param properties.lessonSlug: Human-readable unique ID of the current lesson.
 * @param properties.resourceType: The lesson resources a teacher selected for download.
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/k9ZQJai7ws}
 */
export function resourcesDownloaded(properties: ResourcesDownloadedProperties
  ) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "k9ZQJai7ws", "1e0f78fceef19aca5294e4d6c9882e593e8a00b101914def992a836679518ba3", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Resources Downloaded", {
      "Key Stage Title": properties.keyStageTitle,
      "Key Stage Slug": properties.keyStageSlug,
      "Unit Slug": properties.unitSlug,
      "Unit Name": properties.unitName,
      "Subject Title": properties.subjectTitle,
      "Subject Slug": properties.subjectSlug,
      "Lesson Name": properties.lessonName,
      "Lesson Slug": properties.lessonSlug,
      "Resource Type": properties.resourceType,
      "Analytics Use Case": properties.analyticsUseCase,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("k9ZQJai7ws", "Resources Downloaded", messages, [
      {id: "qeEZpYqVhK", name: "Key Stage Title", value: properties.keyStageTitle},
      {id: "XMx9WMqh0H", name: "Key Stage Slug", value: properties.keyStageSlug},
      {id: "r4GW5No741", name: "Unit Slug", value: properties.unitSlug},
      {id: "YfsvSpyEEd", name: "Unit Name", value: properties.unitName},
      {id: "-MoOjO43sV", name: "Subject Title", value: properties.subjectTitle},
      {id: "8GyPDAapC-", name: "Subject Slug", value: properties.subjectSlug},
      {id: "vbCKXJ6xRQ", name: "Lesson Name", value: properties.lessonName},
      {id: "1FiHL77eSX", name: "Lesson Slug", value: properties.lessonSlug},
      {id: "H_kc7WuVNP", name: "Resource Type", value: properties.resourceType},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Key Stage Title"] = properties.keyStageTitle;
  eventProperties["Key Stage Slug"] = properties.keyStageSlug;
  eventProperties["Unit Slug"] = properties.unitSlug;
  eventProperties["Unit Name"] = properties.unitName;
  eventProperties["Subject Title"] = properties.subjectTitle;
  eventProperties["Subject Slug"] = properties.subjectSlug;
  eventProperties["Lesson Name"] = properties.lessonName;
  eventProperties["Lesson Slug"] = properties.lessonSlug;
  eventProperties["Resource Type"] = properties.resourceType;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Resources Downloaded", {
        "Key Stage Title": properties.keyStageTitle,
        "Key Stage Slug": properties.keyStageSlug,
        "Unit Slug": properties.unitSlug,
        "Unit Name": properties.unitName,
        "Subject Title": properties.subjectTitle,
        "Subject Slug": properties.subjectSlug,
        "Lesson Name": properties.lessonName,
        "Lesson Slug": properties.lessonSlug,
        "Resource Type": properties.resourceType,
        "Analytics Use Case": properties.analyticsUseCase,
        }, "k9ZQJai7ws", "1e0f78fceef19aca5294e4d6c9882e593e8a00b101914def992a836679518ba3");
    }
    // destination PostHog
    PostHog.logEvent("Resources Downloaded", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface KeyStageSelectedProperties {
  keyStageTitle: KeyStageTitleValueType;
  keyStageSlug: string;
  navigatedFrom: NavigatedFromValueType;
  analyticsUseCase: AnalyticsUseCaseValueType[];
}
/**
 * Key Stage Selected: A user chooses the key stage they want to view.
 *
 * When to trigger this event:
 * 1.
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/7ONADbQPez/trigger/bkIRr49k7
 *
 * @param properties the properties associatied with this event
 * @param properties.keyStageTitle: Title of the current key stage.
 * @param properties.keyStageSlug: Human-readable unique ID of the current key stage.
 * @param properties.navigatedFrom: The location of the link used to navigate to the current page
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/7ONADbQPez}
 */
export function keyStageSelected(properties: KeyStageSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "7ONADbQPez", "02b321685570fa9a4ec0aada9f890e9a71ab7a06032b4cd5b0731e09e646314a", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Key Stage Selected", {
      "Key Stage Title": properties.keyStageTitle,
      "Key Stage Slug": properties.keyStageSlug,
      "Navigated From": properties.navigatedFrom,
      "Analytics Use Case": properties.analyticsUseCase,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("7ONADbQPez", "Key Stage Selected", messages, [
      {id: "qeEZpYqVhK", name: "Key Stage Title", value: properties.keyStageTitle},
      {id: "XMx9WMqh0H", name: "Key Stage Slug", value: properties.keyStageSlug},
      {id: "p48bLbzXJ-", name: "Navigated From", value: properties.navigatedFrom},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Key Stage Title"] = properties.keyStageTitle;
  eventProperties["Key Stage Slug"] = properties.keyStageSlug;
  eventProperties["Navigated From"] = properties.navigatedFrom;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Key Stage Selected", {
        "Key Stage Title": properties.keyStageTitle,
        "Key Stage Slug": properties.keyStageSlug,
        "Navigated From": properties.navigatedFrom,
        "Analytics Use Case": properties.analyticsUseCase,
        }, "7ONADbQPez", "02b321685570fa9a4ec0aada9f890e9a71ab7a06032b4cd5b0731e09e646314a");
    }
    // destination PostHog
    PostHog.logEvent("Key Stage Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface SubjectSelectedProperties {
  keyStageTitle: KeyStageTitleValueType;
  keyStageSlug: string;
  subjectTitle: string;
  subjectSlug: string;
  analyticsUseCase: AnalyticsUseCaseValueType[];
}
/**
 * Subject Selected: A teacher chooses the subject they want to view.
 *
 * When to trigger this event:
 * 1. One of the yellow subject cards is selected
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/ufkcMq0HT4/trigger/Pl87P0XPk
 *
 * @param properties the properties associatied with this event
 * @param properties.keyStageTitle: Title of the current key stage.
 * @param properties.keyStageSlug: Human-readable unique ID of the current key stage.
 * @param properties.subjectTitle: Title of the current subject.
 * @param properties.subjectSlug: Human-readable unique ID of the current subject.
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/ufkcMq0HT4}
 */
export function subjectSelected(properties: SubjectSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "ufkcMq0HT4", "95db260a426d378d27d68db9fc53fe1c4ba4426031abd0cf62106775eef779bc", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Subject Selected", {
      "Key Stage Title": properties.keyStageTitle,
      "Key Stage Slug": properties.keyStageSlug,
      "Subject Title": properties.subjectTitle,
      "Subject Slug": properties.subjectSlug,
      "Analytics Use Case": properties.analyticsUseCase,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("ufkcMq0HT4", "Subject Selected", messages, [
      {id: "qeEZpYqVhK", name: "Key Stage Title", value: properties.keyStageTitle},
      {id: "XMx9WMqh0H", name: "Key Stage Slug", value: properties.keyStageSlug},
      {id: "-MoOjO43sV", name: "Subject Title", value: properties.subjectTitle},
      {id: "8GyPDAapC-", name: "Subject Slug", value: properties.subjectSlug},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Key Stage Title"] = properties.keyStageTitle;
  eventProperties["Key Stage Slug"] = properties.keyStageSlug;
  eventProperties["Subject Title"] = properties.subjectTitle;
  eventProperties["Subject Slug"] = properties.subjectSlug;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Subject Selected", {
        "Key Stage Title": properties.keyStageTitle,
        "Key Stage Slug": properties.keyStageSlug,
        "Subject Title": properties.subjectTitle,
        "Subject Slug": properties.subjectSlug,
        "Analytics Use Case": properties.analyticsUseCase,
        }, "ufkcMq0HT4", "95db260a426d378d27d68db9fc53fe1c4ba4426031abd0cf62106775eef779bc");
    }
    // destination PostHog
    PostHog.logEvent("Subject Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface UnitSelectedProperties {
  keyStageTitle: KeyStageTitleValueType;
  keyStageSlug: string;
  subjectTitle: string;
  subjectSlug: string;
  unitSlug: string;
  unitName: string;
  analyticsUseCase: AnalyticsUseCaseValueType[];
}
/**
 * Unit Selected: A teacher chooses the unit they want to view.
 *
 * When to trigger this event:
 * 1. A unit card is selected from the unit listing page
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/Me4ouIgPxh/trigger/t8lbfIsGe
 *
 * @param properties the properties associatied with this event
 * @param properties.keyStageTitle: Title of the current key stage.
 * @param properties.keyStageSlug: Human-readable unique ID of the current key stage.
 * @param properties.subjectTitle: Title of the current subject.
 * @param properties.subjectSlug: Human-readable unique ID of the current subject.
 * @param properties.unitSlug: Human-readable unique ID of the current unit.
 * @param properties.unitName: Title of the current unit.
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/Me4ouIgPxh}
 */
export function unitSelected(properties: UnitSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "Me4ouIgPxh", "e5ee324c7abac44d200825e290fb578095d2e196a7aaeff09b70f1b3ce817aaf", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Unit Selected", {
      "Key Stage Title": properties.keyStageTitle,
      "Key Stage Slug": properties.keyStageSlug,
      "Subject Title": properties.subjectTitle,
      "Subject Slug": properties.subjectSlug,
      "Unit Slug": properties.unitSlug,
      "Unit Name": properties.unitName,
      "Analytics Use Case": properties.analyticsUseCase,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("Me4ouIgPxh", "Unit Selected", messages, [
      {id: "qeEZpYqVhK", name: "Key Stage Title", value: properties.keyStageTitle},
      {id: "XMx9WMqh0H", name: "Key Stage Slug", value: properties.keyStageSlug},
      {id: "-MoOjO43sV", name: "Subject Title", value: properties.subjectTitle},
      {id: "8GyPDAapC-", name: "Subject Slug", value: properties.subjectSlug},
      {id: "r4GW5No741", name: "Unit Slug", value: properties.unitSlug},
      {id: "YfsvSpyEEd", name: "Unit Name", value: properties.unitName},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Key Stage Title"] = properties.keyStageTitle;
  eventProperties["Key Stage Slug"] = properties.keyStageSlug;
  eventProperties["Subject Title"] = properties.subjectTitle;
  eventProperties["Subject Slug"] = properties.subjectSlug;
  eventProperties["Unit Slug"] = properties.unitSlug;
  eventProperties["Unit Name"] = properties.unitName;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Unit Selected", {
        "Key Stage Title": properties.keyStageTitle,
        "Key Stage Slug": properties.keyStageSlug,
        "Subject Title": properties.subjectTitle,
        "Subject Slug": properties.subjectSlug,
        "Unit Slug": properties.unitSlug,
        "Unit Name": properties.unitName,
        "Analytics Use Case": properties.analyticsUseCase,
        }, "Me4ouIgPxh", "e5ee324c7abac44d200825e290fb578095d2e196a7aaeff09b70f1b3ce817aaf");
    }
    // destination PostHog
    PostHog.logEvent("Unit Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface LessonSelectedProperties {
  keyStageTitle: KeyStageTitleValueType;
  keyStageSlug: string;
  unitSlug: string;
  unitName: string;
  subjectTitle: string;
  subjectSlug: string;
  lessonName: string;
  lessonSlug: string;
  analyticsUseCase: AnalyticsUseCaseValueType[];
}
/**
 * Lesson Selected: A teacher chooses a lesson they want to view
 *
 * When to trigger this event:
 * 1. Lesson is selected from listing page
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/iUvld79OSb/trigger/TnGi5eBdB
 *
 * @param properties the properties associatied with this event
 * @param properties.keyStageTitle: Title of the current key stage.
 * @param properties.keyStageSlug: Human-readable unique ID of the current key stage.
 * @param properties.unitSlug: Human-readable unique ID of the current unit.
 * @param properties.unitName: Title of the current unit.
 * @param properties.subjectTitle: Title of the current subject.
 * @param properties.subjectSlug: Human-readable unique ID of the current subject.
 * @param properties.lessonName: Name of the current lesson.
 * @param properties.lessonSlug: Human-readable unique ID of the current lesson.
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/iUvld79OSb}
 */
export function lessonSelected(properties: LessonSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "iUvld79OSb", "e65e224267b2c60043132989f32294c36115396c19c3e304208ccc8154af3c50", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Lesson Selected", {
      "Key Stage Title": properties.keyStageTitle,
      "Key Stage Slug": properties.keyStageSlug,
      "Unit Slug": properties.unitSlug,
      "Unit Name": properties.unitName,
      "Subject Title": properties.subjectTitle,
      "Subject Slug": properties.subjectSlug,
      "Lesson Name": properties.lessonName,
      "Lesson Slug": properties.lessonSlug,
      "Analytics Use Case": properties.analyticsUseCase,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("iUvld79OSb", "Lesson Selected", messages, [
      {id: "qeEZpYqVhK", name: "Key Stage Title", value: properties.keyStageTitle},
      {id: "XMx9WMqh0H", name: "Key Stage Slug", value: properties.keyStageSlug},
      {id: "r4GW5No741", name: "Unit Slug", value: properties.unitSlug},
      {id: "YfsvSpyEEd", name: "Unit Name", value: properties.unitName},
      {id: "-MoOjO43sV", name: "Subject Title", value: properties.subjectTitle},
      {id: "8GyPDAapC-", name: "Subject Slug", value: properties.subjectSlug},
      {id: "vbCKXJ6xRQ", name: "Lesson Name", value: properties.lessonName},
      {id: "1FiHL77eSX", name: "Lesson Slug", value: properties.lessonSlug},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Key Stage Title"] = properties.keyStageTitle;
  eventProperties["Key Stage Slug"] = properties.keyStageSlug;
  eventProperties["Unit Slug"] = properties.unitSlug;
  eventProperties["Unit Name"] = properties.unitName;
  eventProperties["Subject Title"] = properties.subjectTitle;
  eventProperties["Subject Slug"] = properties.subjectSlug;
  eventProperties["Lesson Name"] = properties.lessonName;
  eventProperties["Lesson Slug"] = properties.lessonSlug;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Lesson Selected", {
        "Key Stage Title": properties.keyStageTitle,
        "Key Stage Slug": properties.keyStageSlug,
        "Unit Slug": properties.unitSlug,
        "Unit Name": properties.unitName,
        "Subject Title": properties.subjectTitle,
        "Subject Slug": properties.subjectSlug,
        "Lesson Name": properties.lessonName,
        "Lesson Slug": properties.lessonSlug,
        "Analytics Use Case": properties.analyticsUseCase,
        }, "iUvld79OSb", "e65e224267b2c60043132989f32294c36115396c19c3e304208ccc8154af3c50");
    }
    // destination PostHog
    PostHog.logEvent("Lesson Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface WebinarPageViewedProperties {
  webinarTitle: string;
  webinarCategory: string;
  videoAvailable: boolean;
}
/**
 * Webinar Page Viewed: A user loads a webinar page
 *
 * @param properties the properties associatied with this event
 * @param properties.webinarTitle: Title of the webinar
 * @param properties.webinarCategory: Category of the webinar
 * @param properties.videoAvailable: Watchable video is available on page (eg. not 'upcoming' or 'processing' status
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/-9FHudlATb}
 */
export function webinarPageViewed(properties: WebinarPageViewedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "-9FHudlATb", "a5240d77771020f2e19956f7783bb787d552cee4025f3b9bbcdb059d1fc64680", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Webinar Page Viewed", {
      "Webinar Title": properties.webinarTitle,
      "Webinar Category": properties.webinarCategory,
      "Video Available": properties.videoAvailable,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("-9FHudlATb", "Webinar Page Viewed", messages, [
      {id: "EEuyiIwAD", name: "Webinar Title", value: properties.webinarTitle},
      {id: "MbEsLei6Z", name: "Webinar Category", value: properties.webinarCategory},
      {id: "StT0U3M69", name: "Video Available", value: properties.videoAvailable},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Webinar Title"] = properties.webinarTitle;
  eventProperties["Webinar Category"] = properties.webinarCategory;
  eventProperties["Video Available"] = properties.videoAvailable;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Webinar Page Viewed", {
        "Webinar Title": properties.webinarTitle,
        "Webinar Category": properties.webinarCategory,
        "Video Available": properties.videoAvailable,
        }, "-9FHudlATb", "a5240d77771020f2e19956f7783bb787d552cee4025f3b9bbcdb059d1fc64680");
    }
    // destination PostHog
    PostHog.logEvent("Webinar Page Viewed", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

/**
 * Help Centre Selected: Visit help centre button is clicked on the homepage
 *
 * When to trigger this event:
 * 1. Visit help centre button clicked
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/Ls0Ou1w7qK/trigger/RAhU-otN8
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/Ls0Ou1w7qK}
 */
export function helpCentreSelected() {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "Ls0Ou1w7qK", "3891862271d19ad2784d88ad31e559a410f2146f5dd2d61a01a8635f42b57ff6", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Help Centre Selected", {}, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("Ls0Ou1w7qK", "Help Centre Selected", messages, [], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Help Centre Selected", {}, "Ls0Ou1w7qK", "3891862271d19ad2784d88ad31e559a410f2146f5dd2d61a01a8635f42b57ff6");
    }
    // destination PostHog
    PostHog.logEvent("Help Centre Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface LearningThemeSelectedProperties {
  keyStageTitle: KeyStageTitleValueType;
  keyStageSlug: string;
  subjectTitle: string;
  subjectSlug: string;
  analyticsUseCase: AnalyticsUseCaseValueType[];
  learningThemeName: string;
}
/**
 * Learning Theme Selected: A learning theme filter is applied on a unit listing page
 *
 * When to trigger this event:
 * 1. A learning theme is selected on a unit listing page
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/Vg3AmapucP/trigger/ssM2tCEE3
 *
 * @param properties the properties associatied with this event
 * @param properties.keyStageTitle: Title of the current key stage.
 * @param properties.keyStageSlug: Human-readable unique ID of the current key stage.
 * @param properties.subjectTitle: Title of the current subject.
 * @param properties.subjectSlug: Human-readable unique ID of the current subject.
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 * @param properties.learningThemeName: Name of the learning theme used to group units
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/Vg3AmapucP}
 */
export function learningThemeSelected(
  properties: LearningThemeSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "Vg3AmapucP", "87bec7b1f692bb76f650db6f3437ac639a3af649449d53913dfe5794b2896f0c", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Learning Theme Selected", {
      "Key Stage Title": properties.keyStageTitle,
      "Key Stage Slug": properties.keyStageSlug,
      "Subject Title": properties.subjectTitle,
      "Subject Slug": properties.subjectSlug,
      "Analytics Use Case": properties.analyticsUseCase,
      "Learning Theme Name": properties.learningThemeName,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("Vg3AmapucP", "Learning Theme Selected", messages, [
      {id: "qeEZpYqVhK", name: "Key Stage Title", value: properties.keyStageTitle},
      {id: "XMx9WMqh0H", name: "Key Stage Slug", value: properties.keyStageSlug},
      {id: "-MoOjO43sV", name: "Subject Title", value: properties.subjectTitle},
      {id: "8GyPDAapC-", name: "Subject Slug", value: properties.subjectSlug},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      {id: "-t7ga1V-X", name: "Learning Theme Name", value: properties.learningThemeName},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Key Stage Title"] = properties.keyStageTitle;
  eventProperties["Key Stage Slug"] = properties.keyStageSlug;
  eventProperties["Subject Title"] = properties.subjectTitle;
  eventProperties["Subject Slug"] = properties.subjectSlug;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;
  eventProperties["Learning Theme Name"] = properties.learningThemeName;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Learning Theme Selected", {
        "Key Stage Title": properties.keyStageTitle,
        "Key Stage Slug": properties.keyStageSlug,
        "Subject Title": properties.subjectTitle,
        "Subject Slug": properties.subjectSlug,
        "Analytics Use Case": properties.analyticsUseCase,
        "Learning Theme Name": properties.learningThemeName,
        }, "Vg3AmapucP", "87bec7b1f692bb76f650db6f3437ac639a3af649449d53913dfe5794b2896f0c");
    }
    // destination PostHog
    PostHog.logEvent("Learning Theme Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface TierSelectedProperties {
  subjectTitle: string;
  subjectSlug: string;
  keyStageTitle: KeyStageTitleValueType;
  keyStageSlug: string;
  tierName: string;
  analyticsUseCase: AnalyticsUseCaseValueType[];
}
/**
 * Tier Selected: A user makes a tier choice to show a grouping of units
 *
 * When to trigger this event:
 * 1. On the unit page a Learning tier is selected

NB. This currently only impacts KS4 Maths, but is expected to also impact KS4 Sciences once the new content is introduced
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/6lyU_rQz42/trigger/HHKzuwVa4
 *
 * @param properties the properties associatied with this event
 * @param properties.subjectTitle: Title of the current subject.
 * @param properties.subjectSlug: Human-readable unique ID of the current subject.
 * @param properties.keyStageTitle: Title of the current key stage.
 * @param properties.keyStageSlug: Human-readable unique ID of the current key stage.
 * @param properties.tierName: Name of the learning tier
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/6lyU_rQz42}
 */
export function tierSelected(properties: TierSelectedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "6lyU_rQz42", "281769987465ddd727b8ca8b91f0bc7c74aa08da5ead2c7bafed163b69066e8c", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Tier Selected", {
      "Subject Title": properties.subjectTitle,
      "Subject Slug": properties.subjectSlug,
      "Key Stage Title": properties.keyStageTitle,
      "Key Stage Slug": properties.keyStageSlug,
      "Tier Name": properties.tierName,
      "Analytics Use Case": properties.analyticsUseCase,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("6lyU_rQz42", "Tier Selected", messages, [
      {id: "-MoOjO43sV", name: "Subject Title", value: properties.subjectTitle},
      {id: "8GyPDAapC-", name: "Subject Slug", value: properties.subjectSlug},
      {id: "qeEZpYqVhK", name: "Key Stage Title", value: properties.keyStageTitle},
      {id: "XMx9WMqh0H", name: "Key Stage Slug", value: properties.keyStageSlug},
      {id: "IFuPjLaNs", name: "Tier Name", value: properties.tierName},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Subject Title"] = properties.subjectTitle;
  eventProperties["Subject Slug"] = properties.subjectSlug;
  eventProperties["Key Stage Title"] = properties.keyStageTitle;
  eventProperties["Key Stage Slug"] = properties.keyStageSlug;
  eventProperties["Tier Name"] = properties.tierName;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Tier Selected", {
        "Subject Title": properties.subjectTitle,
        "Subject Slug": properties.subjectSlug,
        "Key Stage Title": properties.keyStageTitle,
        "Key Stage Slug": properties.keyStageSlug,
        "Tier Name": properties.tierName,
        "Analytics Use Case": properties.analyticsUseCase,
        }, "6lyU_rQz42", "281769987465ddd727b8ca8b91f0bc7c74aa08da5ead2c7bafed163b69066e8c");
    }
    // destination PostHog
    PostHog.logEvent("Tier Selected", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface PageViewProperties {
  linkUrl: string;
  pageName: PageNameValueType[];
  analyticsUseCase: AnalyticsUseCaseValueType[];
}
/**
 * Page View: A user views the page
 *
 * @param properties the properties associatied with this event
 * @param properties.linkUrl: The href of a link
 * @param properties.pageName: The main pages of a user journey eg. subject listing, unit listing, etc
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/iYVEEwNT0q}
 */
export function pageView(properties: PageViewProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "iYVEEwNT0q", "f8f7246f065c6330dd9cfd6e4a44842d7bf12159c8f409e89d23958b080bca50", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Page View", {
      "Link URL": properties.linkUrl,
      "Page Name": properties.pageName,
      "Analytics Use Case": properties.analyticsUseCase,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("iYVEEwNT0q", "Page View", messages, [
      {id: "cJ1c_F_Qih", name: "Link URL", value: properties.linkUrl},
      {id: "LgWcfE0FOm", name: "Page Name", value: properties.pageName},
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Link URL"] = properties.linkUrl;
  eventProperties["Page Name"] = properties.pageName;
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Page View", {
        "Link URL": properties.linkUrl,
        "Page Name": properties.pageName,
        "Analytics Use Case": properties.analyticsUseCase,
        }, "iYVEEwNT0q", "f8f7246f065c6330dd9cfd6e4a44842d7bf12159c8f409e89d23958b080bca50");
    }
    // destination PostHog
    PostHog.logEvent("Page View", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export interface ResourceContainerExpandedProperties {
  analyticsUseCase: AnalyticsUseCaseValueType[];
  pageName: PageNameValueType[];
  containerTitle: string;
}
/**
 * Resource Container Expanded: A resource container is expanded to expose the lesson resource content
 *
 * When to trigger this event:
 * 1. A container chevron is expanded to allow a view of the resource.

NB. resource could be attached at a lesson or unit level.
 * View in Avo: https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/B-ti6Ajp8/trigger/57LUx8uOY
 *
 * @param properties the properties associatied with this event
 * @param properties.analyticsUseCase: User is engaging with the site as a pupil or a teacher as defined by the page url (eg. thenational.academy/pupils or thenational.academy/teachers
 * @param properties.pageName: The main pages of a user journey eg. subject listing, unit listing, etc
 * @param properties.containerTitle: The title of the container bar.
eg. Slide deck, Worksheet, Video, Starter quiz, Exit quiz, Unit quiz, Transcript, Lesson overview
 *
 * @see {@link https://www.avo.app/schemas/5PhajbVijwhXVKIJtGMT/branches/cjVJMBMUT/events/B-ti6Ajp8}
 */
export function resourceContainerExpanded(
  properties: ResourceContainerExpandedProperties) {
  // assert properties
  if (__AVO_ENV__ !== AvoEnv.Prod || __WEB_DEBUGGER__) {
    let messages: AvoAssertMessage[] = [];
    // debug console in Avo
    if (!__AVO_NOOP__) {
      _avo_invoke(__AVO_ENV__, "B-ti6Ajp8", "b882d8d3291e25996939ba41a4ac492a73edc35ad80dd7cbf109f8f0f52a29b4", messages.map(m => Object.assign({}, {tag: m.tag, propertyId: m.propertyId, additionalProperties: m.additionalProperties, actualType: m.actualType})), 'event');
    }
    InternalAvoLogger.logEventSent("Resource Container Expanded", {
      "Analytics Use Case": properties.analyticsUseCase,
      "Page Name": properties.pageName,
      "Container Title": properties.containerTitle,
      }, {});
    if (__WEB_DEBUGGER__) {
      // Avo web debugger
      _avo_debugger_log("B-ti6Ajp8", "Resource Container Expanded", messages, [
      {id: "DAS5R4dcvH", name: "Analytics Use Case", value: properties.analyticsUseCase},
      {id: "LgWcfE0FOm", name: "Page Name", value: properties.pageName},
      {id: "o3aX6Ur5U", name: "Container Title", value: properties.containerTitle},
      ], [], []);
    }
  }

  // @ts-ignore
  let eventProperties: any = {};
  eventProperties["Analytics Use Case"] = properties.analyticsUseCase;
  eventProperties["Page Name"] = properties.pageName;
  eventProperties["Container Title"] = properties.containerTitle;

  // @ts-ignore
  let userProperties: any = {};

  if (!__AVO_NOOP__) {
    if (__INSPECTOR__ != null) {
      // @ts-ignore
      __INSPECTOR__._avoFunctionTrackSchemaFromEvent("Resource Container Expanded", {
        "Analytics Use Case": properties.analyticsUseCase,
        "Page Name": properties.pageName,
        "Container Title": properties.containerTitle,
        }, "B-ti6Ajp8", "b882d8d3291e25996939ba41a4ac492a73edc35ad80dd7cbf109f8f0f52a29b4");
    }
    // destination PostHog
    PostHog.logEvent("Resource Container Expanded", (Object as any).assign({}, eventProperties));
  } else {
    // do nothing
  }
}

export default {
  AvoEnv,
  initAvo,
  avoInspectorApiKey,
  VideoLocation,
  AnalyticsUseCase,
  ResourceType,
  PageName,
  NavigatedFrom,
  KeyStageTitle,
  planALessonSelected,
  newsletterSignUpCompleted,
  classroomSelected,
  teacherHubSelected,
  developYourCurriculumSelected,
  supportYourTeamSelected,
  notificationSelected,
  aboutSelected,
  videoStarted,
  videoPaused,
  videoPlayed,
  videoFinished,
  resourcesDownloaded,
  keyStageSelected,
  subjectSelected,
  unitSelected,
  lessonSelected,
  webinarPageViewed,
  helpCentreSelected,
  learningThemeSelected,
  tierSelected,
  pageView,
  resourceContainerExpanded,
}

// AVOMODULEMAP:"Avo"
// AVOEVENTMAP:["planALessonSelected","newsletterSignUpCompleted","classroomSelected","teacherHubSelected","developYourCurriculumSelected","supportYourTeamSelected","notificationSelected","aboutSelected","videoStarted","videoPaused","videoPlayed","videoFinished","resourcesDownloaded","keyStageSelected","subjectSelected","unitSelected","lessonSelected","webinarPageViewed","helpCentreSelected","learningThemeSelected","tierSelected","pageView","resourceContainerExpanded"]
